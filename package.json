{
  "name": "celebconnect-backend",
  "version": "1.0.0",
  "description": "Backend for CelebConnect API",
  "main": "server.js",           // ← adjusted from "src/server.js"
  "scripts": {
    "start": "node server.js",   // ← adjusted accordingly
    "dev": "nodemon server.js"   // optional: for development
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    /* your dependencies here */
  },
  "devDependencies": {
    /* your dev‑dependencies here */
  }
}
